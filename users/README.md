# `users/` — who is applying

Person cards: contact, `characterVoice`, brand pointer.

| Tracked | Gitignored |
|---|---|
| this README · `*.example.json` | real `<id>.json` (PII) |

Copy [`examples/profiles/default-resume/user.example.json`](../examples/profiles/default-resume/user.example.json) → `users/<you>.json`.

The engine also reads the legacy tree `storage/users/` until migration finishes. Layout: [`docs/WORKSPACE-LAYOUT.md`](../docs/WORKSPACE-LAYOUT.md).
