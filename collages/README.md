# `collages/` — image layouts

Collage projects (image sets + candidates). Run `python -m pdf_tool.collage <imagesDir> --recipe <id> --png`.

| Tracked | Gitignored |
|---|---|
| this README | real image sets, `_candidates/`, PNGs |

Legacy alias: `storage/collages/`. Recipes live in tracked `layouts/collage/`. Design: [`docs/COLLAGE-DESIGN.md`](../docs/COLLAGE-DESIGN.md).
