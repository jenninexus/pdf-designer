# `resumes/` — working HTML + exports

Per-person working tree: `resumes/<id>/{html,defaults,_exports,resources}`.

| Tracked | Gitignored |
|---|---|
| this README | HTML, PDFs, `_exports/`, private resources |

Legacy alias: `storage/<id>/`. Shared studio assets stay under `resumes/studio/resources/` (or `brands/`) with the same junction rule as today — see [`docs/STORAGE.md`](../docs/STORAGE.md).
