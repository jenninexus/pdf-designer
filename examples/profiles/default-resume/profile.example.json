{
  "_README": [
    "THE PROFILE — layer ③ of four. Answers: HOW does it render?",
    "",
    "Copy to storage/profiles/<you>-resume.json. That copy is gitignored; this is the shape.",
    "",
    "ONE profile per person. There are NO per-track profile files — a profile per role",
    "track duplicates the vault and drifts out of sync with it. The per-role framing",
    "(what to lead with, what to demote) belongs in the VAULT at roleTracks.<track>.angle.",
    "",
    "So this file holds only what is TRUE REGARDLESS OF TRACK: layout, export paths,",
    "cover-letter policy, theme fallback."
  ],

  "id": "yourname-resume",
  "name": "Your Name — Personal Resume (base profile)",
  "status": "local-private",
  "lastUpdated": "2026-01-01",
  "extends": "../../examples/profiles/default-resume/profile.json",
  "user": "../users/yourname.json",
  "vault": "../yourname/resume-source.json",

  "privacy": {
    "tracked": false,
    "reason": "Real personal resume data. storage/ is gitignored."
  },

  "voice": "One line. The full voice spec — tone, signature moves, what to avoid — lives in the vault's `voice` block. Read it; it matters as much as the facts.",

  "leadIdentity": {
    "_note": "OPTIONAL, but powerful. If your lead identity CHANGES by track (e.g. 'AI specialist' for one kind of role, 'artist' for another), say so here and in the vault's voice.leadIdentityByTrack. Getting this wrong makes a resume feel aimed at the wrong job.",
    "your-track-a": "Who you are FIRST on this track.",
    "your-track-b": "A different lead identity — same person, different framing."
  },

  "roleTracks": {
    "_note": "Defined in the VAULT, not here. This is a pointer, not a second copy.",
    "available": ["your-track-a", "your-track-b"],
    "source": "../yourname/resume-source.json#roleTracks"
  },

  "theme": {
    "base": "../../themes/default-resume.css",
    "default": "Your brand palette — see users/yourname.json -> brandTheme.",
    "mode": "company-derived",
    "modeNote": "Derive the palette from the TARGET COMPANY's real brand CSS when they give a usable signal; store it at applications/<Track>/theme.json (it belongs to the company, not to you). Fall back to your brandTheme otherwise.",
    "pairing": "If two people apply to the SAME company: both render from the company palette with SPLIT accent runs — same grid and type, different accent order — so they read as a matched pair from one studio rather than two copies of one document.",
    "houseRule": "themes/PALETTE-RULES.md — no brown, no mustard, no lime. Enforced on every export."
  },

  "layout": {
    "paper": "Letter",
    "widthIn": 8.5,
    "heightIn": 11,
    "pages": 2,
    "note": "Resume = exactly 2 pages · cover letter = 1 · merged bundle = 3."
  },

  "coverLetterPolicy": {
    "rule": "No separate cover-letter profile — this profile owns its letters. The RESUME stays company-agnostic within a track (so it is reusable across jobs in that track). The COVER LETTER carries ALL company-specific content: the mission hook, their named projects mapped to the listing, the recipient block, and any honest gap framing.",
    "mergeCommand": "python -m pdf_tool.merge_pdfs <out>.pdf <cover-letter-light>.pdf <resume-light>.pdf --require-letter --title \"Your Name - <Role> - Cover Letter and Resume\"",
    "order": "Cover letter FIRST, then resume."
  },

  "exports": {
    "dir": "../yourname/_exports/<Application-Dir>/",
    "rule": "PDFs and PNGs go to storage/<user>/_exports/<Application-Dir>/ — NEVER into the application folder. The application folder holds the listing doc, application.json, theme.json, and the .html sources only. This keeps everything ONE person needs to send in one place.",
    "deliverable": "Unless asked otherwise, an application ships: resume light+dark PDF, cover letter light+dark PDF, and the merged FINAL bundle. PNGs only on request."
  }
}
