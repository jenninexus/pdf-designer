# `_job-apps/_template/` — copy this shape

Public product noun for **this job** is `_job-apps/<Track>/`.

Copy the tracked example, then fill it with *your* listing (gitignored):

[`examples/_job-listings/example-application/`](../../examples/_job-listings/example-application/)
→ `_job-apps/<Track>/`

Do **not** put real listings in `applications/` — that folder is a README redirect
only. Do **not** keep a parallel `storage/_job-listings/` copy.
